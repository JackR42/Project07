# Starter pipeline

# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world, this is a test only!
  displayName: 'Run a one-line script'

- task: TerraformInstaller@0
  displayName: 'Installing Terraform'
  inputs:
    terraformVersion: 'latest'

- task: TerraformTaskV3@3
  displayName: 'Intializing Terraform'
  inputs:
    provider: 'azurerm'
    command: 'init'
    backendServiceArm: 'S2-Visual Studio Ultimate with MSDN(e442b2de-cb70-403f-ac05-5f1d661fb7fa)'
    backendAzureRmResourceGroupName: 'S2-RG-Core-Project07'
    backendAzureRmStorageAccountName: 'saproject07020221025'
    backendAzureRmContainerName: 'containersaproject07020221025'
    backendAzureRmKey: 'main.tf'
  
  
